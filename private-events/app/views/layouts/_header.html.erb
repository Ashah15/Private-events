<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Eventax</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav ml-auto">
      <%= link_to "Home", root_url, {:class => 'nav-link active'}%>
      <%unless logged_in? %>
      	<%= link_to "Sign in", login_path, {:class => 'nav-link'}%>
      <% else %>
      <%= link_to "All Events", events_path , {:class => 'nav-link'}%>
      <%= link_to "User Profile", user_path(current_user) , {:class => 'nav-link'}%>
      <%= link_to "Sign out", logout_path, {:class => 'nav-link', method: :delete } %>
      <% end %>
    </div>
  </div>
</nav>